# Medical Graph RAG 配置文件示例
# 复制此文件为 .env 并填写实际的配置值

# ========== OpenAI API 配置 ==========
# OpenAI API 密钥（必需）
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI API 基础 URL（可选，用于使用代理或自定义端点）
# OPENAI_API_BASE=https://api.openai.com/v1

# 语言模型（可选，默认为 gpt-4o-mini）
# LLM_MODEL=gpt-4o-mini

# 嵌入模型（可选，默认为 text-embedding-3-large）
# EMBEDDING_MODEL=text-embedding-3-large

# ========== Neo4j 图数据库配置 ==========
# Neo4j 连接 URI（可选，默认为 neo4j://localhost:7687）
# NEO4J_URI=neo4j://localhost:7687

# Neo4j 用户名（可选，默认为 neo4j）
# NEO4J_USERNAME=neo4j

# Neo4j 密码（可选，默认为 password）
# NEO4J_PASSWORD=password

# ========== Milvus 向量数据库配置 ==========
# Milvus 连接 URI（可选，默认为 http://localhost:19530）
# MILVUS_URI=http://localhost:19530

# Milvus 认证令牌（可选，用于 Milvus Cloud）
# MILVUS_TOKEN=your-milvus-token-here

# Milvus API 密钥（可选）
# MILVUS_API_KEY=your-milvus-api-key-here

# ========== RAG-Anything 配置 ==========
# RAG-Anything 工作目录（可选，默认为 ./data/rag_storage）
# RAG_WORKING_DIR=./data/rag_storage

# RAG-Anything 工作空间名称（可选，默认为 medical）
# RAG_WORKSPACE=medical

# ========== API 认证配置 ==========
# API Keys（可选，逗号分隔的多个密钥）
# 如果设置了此环境变量，API 将启用认证，需要在请求头中提供 X-API-Key
# 示例: API_KEYS=sk-test-key-1,sk-test-key-2,sk-test-key-3
# API_KEYS=sk-your-api-key-here

# ========== API 速率限制配置 ==========
# 是否启用速率限制（可选，默认为 true）
# RATE_LIMIT_ENABLED=true

# 每个时间窗口内的最大请求数（可选，默认为 100）
# RATE_LIMIT_REQUESTS=100

# 时间窗口长度（秒，可选，默认为 60）
# RATE_LIMIT_WINDOW=60

# ========== 医学领域定制 ==========
# 医学实体类型列表（可选，使用 JSON 格式）
# MEDICAL_ENTITY_TYPES=["DISEASE","MEDICINE","SYMPTOM","ANATOMICAL_STRUCTURE","BODY_FUNCTION","LABORATORY_DATA","PROCEDURE"]
